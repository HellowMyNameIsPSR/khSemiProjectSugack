selectBrandName=SELECT * FROM AUTHOR WHERE AUTHOR_NAME = ?
selectProTypeAll=SELECT TYPE_ID, MATERIAL FROM PROTYPE
insertAuthor=INSERT INTO AUTHOR VALUES (?, ?, ?,  SYSDATE, ?)
insertAuthorType=INSERT INTO AUTHOR_TYPE VALUES (TYPE_ID_SEQ.NEXTVAL, ?, (SELECT TYPE_ID FROM PROTYPE WHERE MATERIAL = ?))
#UPDATE : 1(APPLY_CODE) -> FORIGNE KEY
insertApply1File=INSERT INTO PIC_FILE VALUES (PIC_FILE_SEQ.NEXTVAL, ?, ?, ?, ?, (SELECT APPLY_ID FROM AUTHOR_APPLY WHERE MEMBER_ID = ?), 0)
insertAuthorApply=INSERT INTO AUTHOR_APPLY VALUES (AUTHOR_APPLY_SEQ.NEXTVAL, ?, ?, ?)
selectOneAuthorApply=SELECT M.MEMBER_ID, M.MEMBER_NAME, M.EMAIL, A.AUTHOR_NAME, A.APPLY_CONTENT,AA.APPLY_STAT1, AA.APPLY_STAT2, A.APPLY_DATE, T.CRAFT_ID, T.MATERIAL, F.ORIGIN_NAME, F.CHANGE_NAME, F.FILE_PATH, F.FILE_TYPE FROM MEMBER M JOIN AUTHOR A ON (M.MEMBER_ID = A.MEMBER_ID) JOIN AUTHOR_APPLY AA ON (M.MEMBER_ID = AA.MEMBER_ID) JOIN (SELECT MEMBER_ID, AT.CRAFT_ID, P.MATERIAL FROM AUTHOR_TYPE AT JOIN PROTYPE P ON (AT.CRAFT_ID = P.TYPE_ID)) T ON (A.MEMBER_ID = T.MEMBER_ID) JOIN PIC_FILE F ON (AA.APPLY_ID = F.APPLY_CODE) WHERE A.MEMBER_ID = ?
insertApply2=INSERT INTO PIC_FILE VALUES (PIC_FILE_SEQ.NEXTVAL, ?, ?, ?, ?, (SELECT APPLY_ID FROM AUTHOR_APPLY WHERE MEMBER_ID = ?), 0)
updateAuthorApplyStat2=UPDATE AUTHOR_APPLY SET APPLY_STAT2 = ? WHERE MEMBER_ID = ?
selectSalesForMonth=SELECT SALES, MONTH FROM(SELECT TO_CHAR(PAY_DATE, 'YYYY-MM') AS MONTH, SUM(PRICE) AS SALES FROM BASKET B JOIN ORDER_LIST OL ON(B.BASKET_ID = OL.BASKET_ID) JOIN PAYMENT P ON(P.PAY_ID = OL.PAY_ID) JOIN WORK W ON(W.WORK_ID = B.WID) JOIN MEMBER M ON(M.MEMBER_ID = W.MEMBER_ID) WHERE W.MEMBER_ID = ? AND B.STATUS = 'N' GROUP BY TO_CHAR(PAY_DATE, 'YYYY-MM') ORDER BY MONTH)

selectSalesForMain=SELECT SUM(PRICE) AS SALES, MEMBER_NAME AS AUTHOR, TO_CHAR(P.PAY_DATE, 'RRRR-MM-DD') AS DAY FROM MEMBER M JOIN WORK W ON(W.MEMBER_ID = M.MEMBER_ID) JOIN BASKET B ON(B.WID = W.WORK_ID) JOIN ORDER_LIST OL ON(B.BASKET_ID = OL.BASKET_ID) JOIN PAYMENT P ON(P.PAY_ID = OL.PAY_ID) WHERE B.STATUS = 'N' AND P.PAY_DATE <= (SYSDATE) AND P.PAY_DATE > (SYSDATE-6) AND W.MEMBER_ID = ? AND WORK_KIND = 'SALES' GROUP BY MEMBER_NAME, TO_CHAR(P.PAY_DATE, 'RRRR-MM-DD') ORDER BY 3 DESC
selectSalesForMainFund=SELECT SUM(PRICE) SALES, MEMBER_NAME AUTHOR, TO_CHAR(P.PAY_DATE, 'RRRR-MM-DD') AS DAY FROM MEMBER M JOIN WORK W ON(W.MEMBER_ID = M.MEMBER_ID) JOIN BASKET B ON(B.WID = W.WORK_ID) JOIN ORDER_LIST OL ON(B.BASKET_ID = OL.BASKET_ID) JOIN PAYMENT P ON(P.PAY_ID = OL.PAY_ID) WHERE B.STATUS = 'N' AND P.PAY_DATE <= (SYSDATE) AND P.PAY_DATE > (SYSDATE-1) AND W.MEMBER_ID = ? AND WORK_KIND = 'fund' GROUP BY MEMBER_NAME, TO_CHAR(P.PAY_DATE, 'RRRR-MM-DD') ORDER BY DAY DESC

insertAuthorAcc=INSERT INTO AUTHOR_ACCOUNT VALUES (A_ACCOUNT_SEQ.NEXTVAL, ?, ?, ?, ?, TO_DATE(?, 'YYMMDD'), ?, SYSDATE, ?)
insertApplyList=INSERT INTO APPLY_LIST VALUES (APPLY_LIST_SEQ.NEXTVAL, SYSDATE, ?, (SELECT APPLY_ID FROM AUTHOR_APPLY WHERE MEMBER_ID = ?), ?)
updateAuthorApplyState1=UPDATE AUTHOR_APPLY SET APPLY_STAT1 = ? WHERE MEMBER_ID = ?

